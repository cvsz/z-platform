# Agent Orchestrator

This service owns asynchronous agent-job lifecycle, approval policy, and audit correlation.

It must receive only job references and scoped tool grants. It must not receive wallet signing authority, provider API keys, or browser credentials.
