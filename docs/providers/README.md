# Providers

- [Provider reference](provider-reference.md) — canonical provider keys, documentation, runtime endpoint patterns, protocols, credentials, and AI Gateway compatibility status.

Provider credentials and endpoint defaults are mirrored in the repository root `.env.example`. Runtime enablement still requires live credential, model, streaming, quota, and failover verification.
