# ZOW Workspace

ZOW is the future user-facing workspace layer for Z Platform.

The execution runtime is deliberately separate and is not yet implemented. It must run in a sandbox with explicit approval, resource limits, scoped filesystem access, and no provider or wallet credentials.
